# 口コミコピー導線の検証記録

実施日: 2026-09-10

## 自動チェック

`npm ci` / `npm test`（11件）/ `npm run lint` / `npm run typecheck` / `npm run build` / `git diff --check` 成功。

## ブラウザ検証

ローカルのproduction buildをPlaywrightおよびagent-browserで確認。

| 環境 | 結果 | 範囲 |
| --- | --- | --- |
| PC Google Chrome | 成功 | インストール済みChromeをheadless操作 |
| Android Chrome相当 | 成功 | ChromeのPixel 7エミュレーション。Android実機ではない |
| iPhone Safari相当 | 成功 | WebKit 26.5のiPhone 13エミュレーション。iPhone実機ではない |

各環境で確認した項目:

- アンケート入力、送信、完了画面への遷移
- 改行・前後の空白を含む原文のsessionStorage保存と表示
- 完了画面の再読み込み後も原文を保持
- ユーザー操作から実際のClipboard API書き込みが成功し、成功ダイアログ確認後に既存Google URLへ遷移
- Chromeではクリップボードの読み戻し結果と原文の一致も確認
- コピー拒否とClipboard APIがない状態をテストで再現し、原文保持・再コピーボタン・手動Google遷移を確認
- 空欄・空白のみはコピーせず従来リンクを表示。前回の原文が残らない
- 評価の組み合わせ（待ち時間, スタッフ）: (8,10), (10,8), (8,9), (9,8)は非表示、(9,9), (9,10), (10,9), (10,10)は表示
- JavaScriptの実行時エラーなし

Google URLへの遷移はテスト側で受け止めて検証し、Google画面への入力・投稿は実施していない。

## 表示確認

320 / 375 / 390 / 430 / 768 / 1280pxで入力画面と口コミ表示あり完了画面のスクリーンショットを確認。Header、Hero、全質問、CTA、完了見出し、口コミ案内を確認し、横スクロールなし。失敗時の代替操作もモバイル幅で確認。

## 未実施・実環境での確認手順

iPhone SafariとAndroid Chromeの実機確認は、実機操作環境がないため未実施。上記のエミュレーション結果で代替している。

本番のGAS・Google Sheetsへの実送信は未実施。GASコード、fetchの宛先・POST形式・payload作成処理は変更していない。既存のGAS payload互換性テストは成功。

実機ではHTTPSの公開画面から以下を確認する:

1. 待ち時間9点、スタッフ9点、⑤に複数行のテスト文章を入力して送信する。
2. 完了画面の原文と入力が一致することを確認する。
3. コピーしてGoogleへ進み、Googleの入力欄を長押しして貼り付け、原文一致を確認する。テスト口コミは投稿しない。
4. コピー不可の設定では原文の手動選択・コピーとGoogleへの直接遷移を確認する。
5. ⑤空欄の場合と、どちらかの評価が8点の場合も確認する。
6. 本番GASの保存先で回答が1件保存されていることを確認する。
